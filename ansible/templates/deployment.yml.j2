apiVersion: apps/v1

kind: Deployment

metadata:
  name: java-app
  namespace: {{ namespace }}

spec:

  replicas: 2

  selector:
    matchLabels:
      app: java-app

  template:

    metadata:

      labels:
        app: java-app

    spec:

      containers:

      - name: java-app

        image: {{ image_name }}:{{ image_tag }}

        imagePullPolicy: Never

        ports:

        - containerPort: {{ container_port }}